# dir es para directory 
"""
el modo debug es muy funcional por que puedes ir corriendo el codigo paso a paso 
y que te muestre visualmente muy bien cada variable con que valor tiene en el momento en el que se esta corriendo el codigo
"""
# El comando max es para saber cual es el numero maximo de todos los numeros que le damos 
# El comando minimo es para saber cual es el numero mas chico de todos los numeros que le damos 

# la libreria random se puede usar para obtener un numero random 
"""
import random
numeroAdivinar = random.randint(1,10)
numeroElegido = int(input('Dime un numero del 1 al 10: '))
if numeroElegido == numeroAdivinar:
    print('Felicidades adivinaste el numero!')
else:
    print('Lo siento no adivinaste el numero')
    print('El numero a adivinar era: {}'.format(numeroAdivinar))
"""
# el type se usa para saber de que tipo es la variable 
# el simbolo % es parecido al residuo, funciona de la misma manera

"""
Las variables se pueden castear
se castean como:
int()
float()
entre el parentesis se pone el valor o variable que se quiere castear
"""

"""
Un truco muy bueno para que el codigo se vea mejor es hacer lo siguiente: 
titulo = "Bienvenido a esta encuesta para saber cuanto te gustan los quesos"
print(titulo + '\n' + len(titulo) * '-' + '\n')
lo que hace el print es imprimir la misma cantidad de '-' que caracteres que tiene la variable titulo 
"""

# El ciclo while la mejor forma de traducirlo es como un mientras, mientras tal condicion sea cierta se va a ejecutar lo que este
# dentro del while 